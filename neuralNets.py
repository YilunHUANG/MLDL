def hypo(nn_params,layer_size,X,lambda):
    #hypothesis, the function to predict the finall answer
    '''
    nn_params: unrolled parameter of this neural network
    layer_size: a list containing each layer's size, including the input layer_size
    X: the input examples
    lambda: regularization parameter, deciding the penalty of parameters (weights)
    '''

    #reshape the nn_params

    #compute the finall answer
